cron:
  - description: 'refresh Redis cache'
    schedule: every 10 minutes
    url: /cron/refresh_cache
  - description: 'refresh Dialogflow entities'
    schedule: every 24 hours
    url: /cron/refresh_entities
